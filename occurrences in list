numbers = [1, 2, 3, 2, 4, 2, 5]
element = 2
while element in numbers:
    numbers.remove(element)
print("List after removing all occurrences of", element, ":", numbers)

OUTPUT:
List after removing all occurrences of 2 : [1, 3, 4, 5]

ALTERNATIVE METHOD USING COMPREHENSION LIST:

numbers = [1, 2, 3, 2, 4, 2, 5]
element = 2
numbers = [x for x in numbers if x != element]
print("List after removing all occurrences of", element, ":", numbers)

OUTPUT:
List after removing all occurrences of 2 : [1, 3, 4, 5]
